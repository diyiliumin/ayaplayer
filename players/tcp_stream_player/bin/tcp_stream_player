#!/bin/bash
set -e
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"
BIN="$ROOT/local/tcp_stream_player"
if [ ! -x "$BIN" ]; then
    mkdir -p "$ROOT/local"
    (cd "$ROOT" && go build -o "$BIN" .) >&2
fi
exec "$BIN" "$@"
