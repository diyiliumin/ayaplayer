[package]
name = "ayaplayer-desktop"
version = "0.1.0"
edition = "2021"

[dependencies]
gtk4 = { version = "0.11", features = ["v4_12"] }
libadwaita = "0.9"

[profile.release]
opt-level = 2
lto = "thin"
strip = true
